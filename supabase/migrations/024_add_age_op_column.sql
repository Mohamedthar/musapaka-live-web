-- ===================================================================
-- Migration 024: إضافة عمود age_op إلى جدول competition_levels
-- يحدد نوع شرط العمر: gt, gte, lt, lte, range, all
-- هذا العمود كان مفقوداً وكان سبباً في عدم تطبيق شروط العمر بشكل صحيح
-- ===================================================================
ALTER TABLE competition_levels ADD COLUMN IF NOT EXISTS age_op TEXT;
